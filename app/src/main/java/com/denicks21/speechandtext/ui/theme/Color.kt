package com.denicks21.speechandtext.ui.theme

import androidx.compose.ui.graphics.Color

// — Gradiente principal (monitor) —
val GradientStart = Color(0xFF74409B) // rosa
val GradientEnd   = Color(0xFF384C81)// azul oscuro

// — Estados de alerta —
val AlertOn  = Color(0xFF4CAF50) // verde
val AlertOff = Color(0xFFF44336) // rojo

// — Superficies y fondos —
val LightSurface = Color(0xFFFFFFFF)
val DarkSurface  = Color(0xFF030202)

// — Amarillos/azules secundarios —
val LightYellow = Color(0xFFFFD152)
val DarkYellow  = Color(0xFF017ED0)

// — Grises —
val LightGrey = Color(0xFFCECECE)
val DarkGrey  = Color(0xFF000000)

// — Textos —
val LightText = Color(0xFFFDFDFD)
val DarkText  = Color(0xFF852E2E)

// — Complementarios (errores, acentos, etc.) —
val ErrorColor    = AlertOff
val SuccessColor  = AlertOn
